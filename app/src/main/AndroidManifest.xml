<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="pnu.plato.calendar.app.PlatoCalendarApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_plato_calendar"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_plato_calendar_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PlatoCalendar">
        <activity
            android:name="pnu.plato.calendar.presentation.PlatoCalendarActivity"
            android:exported="true"
            android:theme="@style/Theme.PlatoCalendar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="${ADMOB_APP_ID}" />
    </application>

</manifest>